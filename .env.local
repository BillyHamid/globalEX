# API Backend (suffixe /api obligatoire)
# Développement : backend local = pas de plafond « frais ≤ grille » (cette erreur vient encore de l’API cloud tant qu’elle n’est pas redéployée).
#VITE_API_URL=http://localhost:5000/api

# Prod / tunnel — redéployer le backend depuis ce repo avant d’utiliser, sinon : « Les frais ne peuvent pas dépasser X USD »
VITE_API_URL=https://server.globalexchangeusa.cloud/api
# VITE_API_URL=https://biflex-nonclinically-linn.ngrok-free.dev/api

